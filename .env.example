# JWT Configuration
# Generate a strong secret key (minimum 32 characters)
JWT_KEY=your-super-secret-jwt-key-min-32-chars-long-12345

# AutoMapper License Key (if you have one)
AUTOMAPPER_KEY=

# Database Configuration
# Leave empty to use Docker volume (default, recommended)
# Or specify an absolute path on your host machine for bind mount to access database directly
# Examples:
#   Windows: DATABASE_PATH=C:/Users/Alice/wishlist-data
#   Linux:   DATABASE_PATH=/home/user/wishlist-data
#   Relative: DATABASE_PATH=./.docker-data/wishlist (relative to docker-compose)
DATABASE_PATH=

# API URL for frontend
VITE_API_URL=http://localhost:5000
